'use strict';

var $floor = require('math-intrinsics/floor');

/** @type {import('math-intrinsics/mod')} */
module.exports = function mod(number, modulo) {
	var remain = number % modulo;
	return $floor(remain >= 0 ? remain : remain + modulo);
};
